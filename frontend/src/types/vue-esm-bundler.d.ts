// Full Vue build including the runtime template compiler — required by the
// deck preview runtime, which compiles slide templates from strings.
declare module "vue/dist/vue.esm-bundler.js" {
  export * from "vue";
}
