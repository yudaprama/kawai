// Auto-generated by `kawai-bindings` — DO NOT EDIT.
// Source: kawai-events::LocalChatEvent + kawai-events::AgentChatEvent
// Run: cargo run -p kawai-bindings --bin export-bindings

// This file has been generated by Specta. Do not edit this file manually.
/**
 *  Events from the agentic `agent_chat` stream — the canonical type for
 *  `frontend/src/hooks/use-local-chat.ts:LocalChatEvent`.
 */
export type AgentChatEvent = { type: "started"; sessionId: number } | { type: "token"; text: string } | { type: "toolCall"; id?: string | null; tool: string; args: unknown } | 
/**
 *  On-device model reasoning (thinking mode), streamed live for display.
 *  `text` is a DELTA (append, unlike `SubagentThinking`'s full-buffer
 *  replace). Display-only: never persisted, never enters the conversation.
 */
{ type: "thinking"; text: string } | 
/**
 *  Cloud-subagent reasoning (thinking), streamed live for display.
 *  `text` is the FULL visible buffer so far (replace, not append);
 *  `provider` labels the cloud candidate currently streaming.
 *  Display-only: never persisted, never enters the local conversation.
 */
{ type: "subagentThinking"; provider: string; text: string } | { type: "toolResult"; id?: string | null; tool: string; ok: boolean; summary: string; 
/**
 *  Structured payload for rich UI rendering (tables, cards). Only
 *  set on success paths where the full tool body is available and it
 *  parses as a bounded JSON object/array — otherwise absent and the
 *  frontend renders the summary.
 */
data?: unknown | null } | 
/**
 *  A tool call was intercepted pending explicit user confirmation
 *  (data_import). The UI renders a confirm/cancel card; the user's
 *  reply arrives as the next normal message (the button texts are
 *  backend-composed so the accept wording always satisfies the gate).
 */
{ type: "confirmationRequest"; tool: string; 
/**  Human-readable summary of what needs confirming. */
prompt: string; 
/**  Pre-built reply for the UI's accept button. */
acceptText: string; 
/**  Pre-built reply for the UI's decline button. */
declineText: string } | { type: "finished" } | { type: "error"; message: string };

/**  Events from the raw on-device `local_chat` stream (legacy / `local-llm`). */
export type LocalChatEvent = { type: "started" } | { type: "token"; text: string } | 
/**
 *  Non-final reasoning text (thinking mode) — surfaced for the UI toggle
 *  and telemetry, never part of the answer.
 */
{ type: "thinking"; text: string } | { type: "toolCall"; id?: string | null; tool: string; args: unknown } | { type: "toolResult"; id?: string | null; tool: string; ok: boolean; summary: string } | { type: "finished" } | { type: "error"; message: string };
