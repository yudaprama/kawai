---
target: frontend/
total_score: 28
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-09-04T15-19-06Z
slug: frontend
---
# Design Critique — frontend/ (kawai React SPA)

Method: sequential single-context (degraded: no sub-agent tool exposed). A: inline design review (App.tsx, assets-rail.tsx, conversation-panel.tsx, chat-composer.tsx, plan-progress-panel.tsx, index.css, asset-nav.tsx). B: detect.mjs CLI (0 findings) + browser skipped (no approvable browser in session).

## Design Health Score — 28/40 (Good)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4 | Excellent: live plan steps, parallel count, model warm-up, Thinking pill |
| 2 | Match System / Real World | 3 | Mixed EN/ID prompt chips; "handle:" jargon |
| 3 | User Control and Freedom | 3 | Stop/Esc/cancel everywhere; chatError has no dismiss |
| 4 | Consistency and Standards | 2 | Raw palette bypass (emerald-500/blue-500); DatabaseIcon on every confirmation; dead affordance |
| 5 | Error Prevention | 3 | Confirmation gate; Approve/Reject disabled while busy |
| 6 | Recognition Rather Than Recall | 2 | History only via icon-only button + modal |
| 7 | Flexibility and Efficiency | 3 | ⌘1/⌘2/⌘K, Esc layering, Ctrl+F virtualization escape |
| 8 | Aesthetic and Minimalist Design | 3 | Calm and uncluttered but generic |
| 9 | Error Recovery | 3 | Retry on model/history; chatError names problem, no action |
| 10 | Help and Documentation | 2 | Good empty-state copy; no contextual help after first run |

## Design Specificity Verdict
Category-interchangeable. Tea enterprise blue + default shadcn = generic B2B console. The product's differentiator — a plan executing live — is rendered as the flattest element (bg-card box, 10–11px muted text, no progress geometry). Detector: 0 findings. No visual overlays (browser automation unavailable in session).

## Priority Issues
- [P1] Confirmation card mislabeled: every supervisor confirmation renders "Import confirmation" + DatabaseIcon regardless of actual action. Fix: derive label/icon from confirmation.tool; warning styling for destructive actions. → $impeccable harden
- [P1] Dead affordance: StructuredArtifact chevron + "structured result" has no click handler. Fix: wire expandable JSON or remove chevron. → $impeccable harden
- [P2] Plan panel flat + off-token: raw emerald-500/blue-500 bypass Tea tokens; no hierarchy between goal/running/artifacts. Fix: tokenize step-state colors, progress affordance, promote artifacts. → $impeccable bolder
- [P2] DESIGN.md describes unshipped app (3-agent picker, persistent 240px sessions panel, per-agent tabs) vs built auto-mode single-agent; mixed EN/ID copy in builtin.analytics prompts. Fix: rewrite DESIGN.md; pick one language. → $impeccable document
- [P2] Session history has no persistent home (icon-only button + modal only). Fix: slim persistent rail at xl+, modal below. → $impeccable shape

## Persona Red Flags
- Alex: prompt chips auto-submit a full plan run with no edit chance — should drop text into composer.
- Jordan: mislabeled confirmation invites blind approval; "handle:"/"structured result" jargon; no help past empty state.
- Sam: 23 size="icon" buttons rely on title only; plan state icon+color but running = spinner only.

## Minor Observations
- chatError: no dismiss/retry/link.
- "New Task" uses ListPlusIcon — wrong mental model; PlusIcon fits.
- Asset rail active state hardcodes --tea-color-bg-brand-default instead of bg-primary.
- Confirmation prompt truncated with only native title tooltip.

## Questions to Consider
- What if the live plan were the visual centerpiece of the app?
- Does the header need two entry points to sessions?
- What is kawai's own color, if not default Tea blue?
