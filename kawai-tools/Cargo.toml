[package]
name = "kawai-tools"
version = "0.1.0"
edition = "2024"
description = "The kawai agent tool seam: typed AgentTool trait + name-keyed ToolSet registry with JSON-args dispatch"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
thiserror = "2"
